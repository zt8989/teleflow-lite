# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Packaging output (PyInstaller build dir, frozen app, DMG/EXE installer)
packaging/macos/build/
packaging/macos/dist/
packaging/macos/dmgroot/
*.dmg
*.exe

# Tooling
.pytest_cache/
.mypy_cache/

# Local config / logs written at runtime
.config/
*.log

# Git worktree isolation directory
.worktrees/

# Vendored, machine-specific prebuilt wheels live in dist/ (built by scripts/build_pjsua2_wheel.py).
# dist/ is already ignored above; wheel files are not committed, the build script is.
